<!DOCTYPE html>
<html>
<head>
  <title>StacheOverflow</title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <link href='http://fonts.googleapis.com/css?family=Quicksand' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div id="nav-bar">
      <ul>
        <li>
          <% if @username %>
            <p><%= link_to "Logout", '/logout' %> <%= @username %> </p>
          <% else %>
            <%= link_to "Login With Twitter", '/auth/twitter' %>
          <% end %>
        </li>
      </ul>
    </div>
    <%= image_tag "mustache.jpg" %>
    <h1>
      <%= link_to "Stache Overflow", :root %>
    </h1>
    <br>
  </header>
<%= yield %>

</body>
</html>
